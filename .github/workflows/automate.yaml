name: Automated Build Actions
on: [push]
jobs:
    setup-and-build:
        runs-on: ubuntu-latest
        steps:
            - run: npm install
            - run: npm run pret
            - run: npm run lint
            - run: npm run test
            - run: npm run tsc
            - run: npm run build